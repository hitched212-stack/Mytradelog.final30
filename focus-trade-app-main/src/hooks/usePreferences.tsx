// Re-export everything from the context for backwards compatibility
export { 
  usePreferences, 
  PreferencesProvider,
  type Theme,
  type TextSize,
  type AccentColor,
  type GoalPeriod,
  type ColorPreset,
  type Preferences,
} from '@/contexts/PreferencesContext';
